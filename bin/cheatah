#!/usr/bin/env node

var fs = require('fs')
var pkg = require('../package.json')
var program = require('commander')
var Cheatah = require('../')

program
.version(pkg.version)
.option('-t, --template', 'your template file (.ejs) path')
.option('-s, --stylesheet', 'your stylesheet path')
.parse(process.argv)
