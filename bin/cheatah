#!/usr/bin/env node

var fs = require('fs')
var pkg = require('../package.json')
var program = require('commander')
var Cheatah = require('../')

program
.version(pkg.version)
.option('-t, --template', 'import template file (.ejs)')
.option('-s, --stylesheet', 'import stylesheet')
.parse(process.argv)
